# Selected-tier routing

Establish the selected tier from the actual entitled package or a known verified installation/entitlement receipt. Read its `ENTITLEMENTS.json` and preserve the matching project. The local package describes its boundary; it does not prove a live connector session or authorize an account mutation. A current authenticated entitlement refusal remains authoritative. Never infer privileges from available tools, folder names, a trial request or a shared brand display name.

| Actual selected tier | Local guide | Data and operating boundary |
|---|---|---|
| `gls_plus` / served slug `gls-plus` | `AGENT_START.md` names `GLS_GUIDED_SETUP.py` | 47 retained skills. Entitled connected Amazon Ads route; connection unverified until proven. Seller Central/SP-API, catalog, listing, inventory, business, fees, returns and SQP evidence are manual. Use `GLS_PLUS_CONFIG.json` and the retained manual engine. No VIP machine, connected SP-API, installed schedules, verified-dollar ledger or second trial. |
| `vip` | `AGENT_START.md` names `GUIDED_SETUP.py` | Retain the VIP one-account machine, source and identity gates, manual canaries and exact schedule-plan approval. Launch Cockpit remains subject to current authorization and host availability; its UI does not complete VIP operations. |
| Unknown, conflicting or unbound | Read existing rules/memory and answer general education | Obtain actual selected-tier evidence before restricted business actions. Do not choose VIP because the connector exposes more tools, use the cockpit as an entitlement test, switch an account or create another introductory-week state. |

Use the helper named by the selected entrypoint with the original client's folder. A separately provided verified companion must be for that same tier; do not copy it over the client. Preserve already supplied brand details and actual first-week progress. Ask only for the next missing fact or real scope conflict.

For GLS launch questions, select the relevant retained market-research, listing or creative skill and its manual evidence. For GLS cadence questions, guide the manual checklist without creating a scheduler task. Ordinary requests do not require full-kit sync or loading all 47/50 skill bodies.
